from setuptools import find_packages, setup

setup(
    name="medbot",
    version="0.1.0",
    author="Sanjiv",
    description="A medical chatbot powered by LangChain, OpenAI",
    packages=find_packages(),
    install_requires=[],
)
